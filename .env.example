# En production (Railway, Render, etc.) :
# DATABASE_URL = l'URL PostgreSQL fournie par l'hébergeur
# JWT_SECRET = une longue string aléatoire (ex: générée avec `openssl rand -hex 32`)

DATABASE_URL="postgresql://user:password@localhost:5432/gendoc"
JWT_SECRET="change-me-to-a-long-random-string"
